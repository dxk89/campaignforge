{
  "name": "campaign-forge",
  "version": "0.1.0",
  "description": "Agentic marketing campaign generator: brief in, multi-channel assets out, with visible cost economics.",
  "main": "server.js",
  "private": true,
  "scripts": {
    "start": "node legacy/server.js",
    "dev": "node --watch server.js",
    "test": "node test/runtime.test.js && node test/db.test.js && node test/export.test.js && node test/api.test.js && node test/pages.test.js && node test/frontend.test.js",
    "web": "cd web && next dev",
    "web:build": "cd web && next build",
    "legacy": "node legacy/server.js",
    "test:emulator": "cd web && npx firebase emulators:exec --only firestore --project demo-cf \"cd .. && FIRESTORE_EMULATOR_HOST=127.0.0.1:8080 GCLOUD_PROJECT=demo-cf node test/db.test.js\""
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "jsdom": "^30.0.1"
  }
}
